#! /bin/bash # TEN PROGRAM NIE DZIAŁA ;( zrobic inaczej
	
	for basename in `ls` #to są znaki nad tyldą na klawiaturze
		
			
			if [ $zm -en != $basename `$0` ]
			cp $zm $zm.old
			echo  
			fi
		done
	exit 0
	
#wpisanie w terminalu {echo $?} po wykonaniu skryptu w terminalu skutkuje wyrzsuceniem w terminalu 0 <--wynik działnia exit
